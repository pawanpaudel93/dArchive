type Url @entity {
  id: Bytes!
  url: String!
  archives: [Archive!]! @derivedFrom(field: "contentURL")
}

type Archive @entity {
  id: ID!
  timestamp: BigInt!
  title: String!
  contentID: String!
  contentURL: Url!
}
